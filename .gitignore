# GoogleService-Info (Firebase API 키 포함 — 절대 커밋 금지)
GoogleService-Info.plist

# 서버 환경변수 / 시크릿 (절대 커밋 금지)
.env
.env.*
!.env.example

# Xcode
*.xcuserstate
*.xcuserdatad
xcuserdata/
DerivedData/
*.moved-aside
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3

# Swift Package Manager
.build/
.swiftpm/

# macOS
.DS_Store

# CocoaPods
Pods/

# Firebase Functions
functions/node_modules/
functions/lib/
functions/firebase-debug.log

# 마케팅 자산 (로컬 전용 — 저장소에 넣지 않는다)
#  out/    : render.sh로 언제든 재생성되는 PNG
#  fonts/  : Pretendard 중복본 (tteona/Fonts·assets/fonts에 이미 있음)
marketing/
